{
    "name": "tree-sitter-norg4",
    "version": "0.0.1",
    "description": "tree-sitter parser for Norg format",
    "main": "bindings/node",
    "scripts": {
        "generate": "./tree-sitter-patch generate",
        "test": "./tree-sitter-patch generate && ./tree-sitter-patch test",
        "parse": "./tree-sitter-patch generate && ./tree-sitter-patch parse",
        "size": "du -h ~/.cache/tree-sitter/lib/norg*.so"
    },
    "keywords": [
        "tree-sitter",
        "neorg",
        "norg"
    ],
    "author": "boltlessengineer",
    "license": "ISC",
    "devDependencies": {
        "tree-sitter-cli": "^0.20.8"
    }
}
